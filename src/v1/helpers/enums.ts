export enum AuthHeaders {
  Authorization = 'authorization',
  ResourceType = 'resourceType',
  Company = 'company',
  Origin = 'origin',
}

export enum Route {
  auths = 'auth',
  companies = 'companies',
}
